//! Shared Phase 3 metadata types used by the processor and native model.

pub use candle_transformers::models::lfm2_vl::{
    CropKind, CropMeta, EncodedImages, ImageMeta, ImageTokenSpan, ProcessedVisionBatch,
};
