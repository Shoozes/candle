# GPT-OSS Task 2 independent oracle

- Fixture ID: `synthetic-gpt-oss-task2-oracle-v1`
- Raw fixture: `oracle.json`
- Raw bytes: 2,690
- SHA-256: `db79441a688bce500217635051372270fce02b7e6f8f03d769855ac290d4ab04`
- Absolute tolerance: `1e-4`

The expected packed-expert, router, attention, uncached-forward, and cached
decode values were generated by a standalone scalar Python/Numpy reference,
not by the Candle implementation under test.  The pinned environment was
Python `3.13.3` with NumPy `2.3.3`.  Inputs use the existing deterministic
synthetic model seeds (`1`, `7`, and linear seeds `0.0001` through `0.0004`).

The equations are behavior-pinned to OpenAI `gpt-oss` commit
`7b583341fe16729127f6d5b94a7b09ccae97e1a1` and llama.cpp commit
`f072b103714dfa1eee531f80b24512faf38e3dd2`.  The Rust test verifies the raw
fixture digest before comparing values, and checks both uncached prompt logits
and the cached prefill/decode transition.

This is a bounded synthetic numerical receipt only.  It is not a production
checkpoint, tokenizer, exact-model parity claim, or CUDA reference.
